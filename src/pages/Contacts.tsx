import MapContainer from "../components/GoogleMap";

const Contacts = () => {
  return (
    <section className={"section"}>
      Ми тут
      <MapContainer />
    </section>
  );
};

export default Contacts;
