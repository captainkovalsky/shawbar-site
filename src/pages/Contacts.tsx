import MapContainer from "../components/GoogleMap";

const Contacts = () => {
  return (
    <section className={"section"}>
      Contact
      <MapContainer />
    </section>
  );
};

export default Contacts;
